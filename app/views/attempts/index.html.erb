<% content_for :title, "Attempts" %>

<h1>Attempts</h1>

<div id="attempt">
  <% @attempts.each do |attempt| %>
    <%= render attempt %>
    <p>
      <%= link_to "Show this attempt", test_attempt_path(@test, attempt) %>
    </p>
  <% end %>
</div>
